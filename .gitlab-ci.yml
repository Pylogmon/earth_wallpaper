image: ubuntu:20.04

before_script:
  - apt update
  - apt install -y cmake gcc make
  - apt install -y software-properties-common
  - add-apt-repository ppa:beineri/opt-qt-5.15.3-focal
  - apt update
  - apt install -y qt515base

  
build:
  script:
    - mkdir build && cd build
    - cmake ..
    - make
    - cd ../package
    - ./package.sh
  artifacts:
    paths:
      - earth-wallpaper-amd64.deb
